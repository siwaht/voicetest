langchain
langgraph
deepagents
langchain-openai
langchain-text-splitters
python-dotenv
elevenlabs
langchain-quickjs
# agent.py imports `livekit.agents` plus the langchain and silero plugins.
# Without these the process crashes on import with ModuleNotFoundError.
# The extras pull in livekit-plugins-langchain and livekit-plugins-silero.
# turn-detector is required by `expressive=True` on the AgentSession and must
# match the extras in pyproject.toml, since a deploy may build from either file.
livekit-agents[langchain,silero,turn-detector]>=1.7.0